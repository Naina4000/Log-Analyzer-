# Log-Analyzer-
Here’s a shorter version:  > Built a Python log analyzer that parses system logs to detect brute-force attempts, suspicious IP activity, and abnormal logins using rule-based detection and regex, generating alerts similar to a SIEM monitoring system.
